<!DOCTYPE html>
<html>
  <head>
    <title>Rails - Users</title>
  </head>

  <body>
    <h1> Users </h1>
    <% @user.each do |u| %>
        <p><%= link_to u.first_name, user_path(u.id) %> </p>
    <% end %>

    <%= link_to "New User", "users/new"%>
  </body>
</html>